# conditional statement ?
# In real life, we make decision based on conditional puthon works the some way.

# if statement?
# The if statement is the most baisc form . is the condion is True, the indented block runs. if is False ,python simple skip it.else

# else statement?
# Use else when you want something to happen when the codition is False. one of the two blocks will always run-never both, 
# never neither.

# elif statement?
# When you have more then two possible outcomes, use elif (short for "else if").
# Python checks each condition from top to bottom and runs the first one that is True. the rest are skipped entirely.

# Nested if-else statement?
# You can place an if statement inside another if statement . This is called nesting and if the first one is already True.